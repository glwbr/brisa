//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  printWidth: 120,
  useTabs: false,
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
